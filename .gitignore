# Ignore all files with 'secret' in the name
*secret*

# But ONLY allow files that START with 'sealed-secret'
!sealed*

# Optional: additional secret-related exclusions
**/*.secret
**/*.key
**/*.pem

# Ignore environment files
.env
*.env
.env.*

# Ignore all charts directories and Chart.lock files
**/charts/
**/Chart.lock